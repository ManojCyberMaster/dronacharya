__pycache__/
*.pyc
.venv/
venv/
dist/
build/
*.egg-info/
.pytest_cache/
*.db
*.db-wal
*.db-shm
.env
.DS_Store

# server deployment state (holds real tokens) — never commit
server-data/
# built seed kits (publish as release assets, not in-tree)
*.dckit.json
# live searxng/settings.yml is UNTRACKED (holds your secret_key; copy it
# from the tracked example, then git never touches it again)
searxng/*
!searxng/settings.example.yml
